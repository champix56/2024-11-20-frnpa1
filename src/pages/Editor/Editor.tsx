import React from 'react';
import {useSelector} from 'react-redux';
import ProductForm from '../../components/ProductForm/ProductForm.connected';

const Editor = () => {
  return <ProductForm />;
};

export default Editor;
