export const REST_HOST = 'localhost';
export const REST_PORT = '5679';
export const REST_ADRESSE = `http://${REST_HOST}:${REST_PORT}`;
export const RESSOURCES = Object.freeze({products: '/products'});
